/**
 * توکن‌های ظاهری — بر پایهٔ سیستم دیزاین iOS.
 *
 * نردبان ink مستقیم روی خاکستری‌ها و برچسب‌های سیستمی اپل نگاشته شده.
 * چون ویوها همه‌جا از همین کلاس‌ها استفاده می‌کنند (text-ink-900،
 * border-ink-200 و…)، عوض کردن این جدول یعنی عوض شدن کل ظاهر برنامه
 * بدون دست زدن به تک‌تک صفحه‌ها.
 *
 * یک تصمیم که باید صریح باشد: رنگ‌های استانداردِ iOS با متن سفید از
 * WCAG AA رد نمی‌شوند — systemBlue روی سفید ۴٫۰۲ است و systemGreen
 * ۲٫۲۲. پس هرجا متن روی رنگ می‌نشیند از جدول «Increase Contrast»
 * خود اپل استفاده شده، نه از رنگ پیش‌فرض.
 */
module.exports = {
  content: [
    '../../resources/views/**/*.php',
    '../../app/**/*.php',
    '../../public/**/*.php',
  ],
  /*
   * کلاسی، نه مدیاکوئری: کاربر خودش روشن/تیره را انتخاب می‌کند و
   * انتخابش در localStorage می‌ماند. با 'media' کلید تغییرِ حالت
   * بی‌اثر می‌شد چون تیلویند به تنظیم سیستم‌عامل نگاه می‌کرد.
   */
  darkMode: 'class',
  theme: {
    extend: {
      fontFamily: { sans: ['Vazirmatn', 'Tahoma', 'sans-serif'] },
      colors: {
        /*
         * خاکستری‌های سیستمی اپل + برچسب‌ها.
         *   ۵۰–۳۰۰  سطح و جداکننده (systemGray6 تا systemGray3)
         *   ۴۰۰–۵۰۰ متن کم‌رنگ
         *   ۶۰۰–۹۵۰ متن اصلی (تا label)
         *
         * پله‌های ۴۰۰ و ۵۰۰ عمداً از systemGray2/systemGray اپل
         * تیره‌ترند. اپل آن دو را برای fill و جداکننده می‌سازد، ولی
         * ویوهای ما با همان‌ها متنِ راهنما می‌نویسند — و systemGray2
         * روی سفید ۲٫۲۱ است. بازرسی صفحه‌ها ۷۳ مورد متنِ کم‌کنتراست
         * پیدا کرد که تقریباً همه از همین دو پله می‌آمدند.
         */
        ink: {
          50:'#F2F2F7', 100:'#E5E5EA', 200:'#D1D1D6', 300:'#C7C7CC',
          400:'#5F5F63', 500:'#545458', 600:'#48484A', 700:'#3A3A3C',
          800:'#2C2C2E', 900:'#1C1C1E', 950:'#000000',
        },
        /*
         * systemYellow/Orange. فقط برای تأکید و هشدار.
         * ۶۰۰ به بالا نسخهٔ accessible است، چون متن رویشان می‌نشیند.
         */
        gold: {
          50:'#FFF8E6', 100:'#FFEFC2', 200:'#FFE08A', 300:'#FFD426',
          400:'#FFCC00', 500:'#FF9500', 600:'#B25000', 700:'#8F4000',
          800:'#6B3000', 900:'#452000',
        },
        // brand به خاکستریِ سیستمی نگاشته می‌شود تا کلاس‌های قدیمی نشکنند
        brand: {
          50:'#F2F2F7', 100:'#E5E5EA', 500:'#8E8E93',
          600:'#1C1C1E', 700:'#000000', 900:'#000000',
        },
      },
      boxShadow: {
        /*
         * iOS سایه را کم و کوتاه می‌گذارد. کارت‌های grouped اصلاً سایه
         * ندارند و با اختلاف رنگِ سطح از زمینه جدا می‌شوند؛ سایه فقط
         * برای چیزهایی است که واقعاً «شناورند» (شیت، نوار چسبیده).
         */
        'card': '0 1px 2px 0 rgb(0 0 0 / 0.04)',
        'lift': '0 2px 8px -2px rgb(0 0 0 / 0.10)',
        'deep': '0 8px 28px -8px rgb(0 0 0 / 0.18)',
      },
      /*
       * پیش‌فرضِ حلقهٔ فوکوس. بدون این، ring-2 آبیِ پیش‌فرضِ تیلویند را
       * می‌گذاشت و چون بعد از ring-accent می‌آمد، روی آن می‌نشست.
       */
      ringColor: { DEFAULT: 'var(--accent)' },
      ringOffsetColor: { DEFAULT: 'var(--surface)' },
      /*
       * گردی‌های iOS: ۱۰ برای کنترل‌ها، ۱۲–۱۴ برای دکمه‌ها،
       * ۱۶ برای بلوک‌های grouped. گوشهٔ پیوسته‌نما با squircle
       * شبیه‌سازی می‌شود، نه با شعاع‌های بزرگ‌تر.
       */
      borderRadius: { 'xl': '0.75rem', '2xl': '0.875rem', '3xl': '1.25rem' },
      transitionTimingFunction: {
        /*
         * منحنی حرکت iOS. اپل از یک spring استفاده می‌کند که شروعش
         * تند است و آخرش آرام می‌نشیند — نه ease-out معمولی که در
         * تمام مسیر یکنواخت کند می‌شود.
         */
        'out-soft': 'cubic-bezier(0.32, 0.72, 0, 1)',
        'ios': 'cubic-bezier(0.32, 0.72, 0, 1)',
      },
    },
  },
  plugins: [],
};
