# ۰۱ — نمای کلی معماری

---

## ۱. اصول

پنج اصلی که هر تصمیم فنی در برابرشان سنجیده می‌شود:

1. **همه‌چیز داخل ایران.** هیچ وابستگی زمان‌اجرا به سرویس خارجی. ([R6](../00-product/09-risks.md))
2. **یک دیتابیس، چندمستأجره.** چون فاز ۴ مارکت‌پلیس است و جستجوی بین‌سالنی باید یک `SELECT` باشد.
3. **ساده تا وقتی که درد بگیرد.** ۱۰۰ سالن با یک سرور و MySQL کار می‌کند. معماری میکروسرویس در
   این مقیاس فقط هزینه است.
4. **دامنه‌محور، نه MVC خام.** منطق صف و تسویه پیچیده می‌شود؛ اگر در کنترلر بنویسیم، شش ماه دیگر
   قابل تغییر نیست.
5. **موبایل‌اول و آفلاین‌مدارا.** اینترنت سالن قطع و وصل می‌شود.

---

## ۲. استک

| لایه | انتخاب | چرا |
|---|---|---|
| زبان | PHP 8.3 | مهارت موجود تیم؛ هاست ایرانی همه‌جا داردش |
| فریم‌ورک | **Laravel 12** | اکوسیستم، صف کار، زمان‌بند، Sanctum |
| دیتابیس | **MySQL 8** | در دسترس روی هر هاست ایرانی |
| کش و صف | **Redis** (روی VPS) / `database` (روی اشتراکی) | هر دو مسیر پشتیبانی می‌شود |
| پنل سالن | **Livewire 3 + Alpine** | بدون SPA جدا؛ سرعت توسعهٔ بالا برای تیم کوچک |
| صفحهٔ مشتری | **Blade + Alpine، به‌شکل PWA** | سبک، بدون نصب، روی 3G هم باز می‌شود |
| بی‌درنگ | فاز ۱: **پولینگ ۱۵ ثانیه‌ای با ETag**<br>فاز ۲: **Laravel Reverb** | پولینگ روی هاست اشتراکی هم کار می‌کند. Reverb خودمیزبان است (نه Pusher) |
| جستجو | MySQL FULLTEXT → فاز ۴: Meilisearch | |
| فایل | دیسک محلی → فاز ۳: آبجکت‌استوریج ایرانی | |
| پوش | **Web Push (VAPID)** | بدون Firebase |
| تست | Pest | |
| CI | GitHub Actions (با کش وابستگی‌ها) | |

**دو چیزی که عمداً انتخاب نکردیم:**
- **SPA جدا (Vue/React) برای پنل** — تیم کوچک، دو کدبیس، دو برابر کار. Livewire همان کار را می‌کند.
- **PostgreSQL** — بهتر است، ولی هاستینگ ایرانی MySQL‌محور است و مهاجرتش ارزش دردسرش را ندارد.

---

## ۳. نمای اجزا

```
┌──────────────┐   ┌──────────────┐   ┌──────────────┐   ┌─────────────┐
│  مشتری       │   │  آرایشگر     │   │ صاحب/پذیرش  │   │  پلتفرم (ما) │
│  PWA، بدون   │   │  PWA موبایل  │   │  پنل وب      │   │  پنل ادمین  │
│  نصب و رمز   │   │              │   │              │   │             │
└──────┬───────┘   └──────┬───────┘   └──────┬───────┘   └──────┬──────┘
       │                  │                  │                  │
       └──────────────────┴────────┬─────────┴──────────────────┘
                                   │  HTTPS
                        ┌──────────▼───────────┐
                        │   Laravel 12         │
                        │  ┌────────────────┐  │
                        │  │ Http (وب/API)  │  │
                        │  ├────────────────┤  │
                        │  │  Domain/       │  │
                        │  │   Queue ★      │  │  ← قلب محصول
                        │  │   Booking      │  │
                        │  │   Customer     │  │
                        │  │   Payout       │  │
                        │  │   Messaging    │  │
                        │  │   ...          │  │
                        │  ├────────────────┤  │
                        │  │ Tenancy (اسکوپ)│  │
                        │  └────────────────┘  │
                        └───┬───────┬──────┬───┘
                            │       │      │
                  ┌─────────▼─┐ ┌───▼───┐ ┌▼─────────┐
                  │  MySQL 8  │ │ Redis │ │ Storage  │
                  └───────────┘ └───┬───┘ └──────────┘
                                    │ صف کار
                       ┌────────────┼─────────────┐
                       ▼            ▼             ▼
                 ┌──────────┐ ┌──────────┐ ┌────────────┐
                 │ پیامک    │ │ درگاه    │ │ محاسبهٔ ETA│
                 │ملی‌پیامک │ │ زیبال    │ │ + آمار مدت │
                 │ +پشتیبان │ │+زرین‌پال │ │            │
                 └──────────┘ └──────────┘ └────────────┘
```

---

## ۴. چرا `Domain/`

منطق کسب‌وکار در پوشهٔ `app/Domain/` و هر دامنه خودبسنده است. دلیلش سلیقه نیست:

- **موتور صف و ETA** ([۰۴](04-queue-eta-engine.md)) پیچیده‌ترین بخش است و باید بدون HTTP قابل تست باشد
- **تسویه** قواعد مالی دارد؛ اشتباهش پول واقعی است
- فاز ۴ احتمالاً بعضی دامنه‌ها را جدا می‌کند؛ مرز از قبل کشیده باشد

ساختار هر دامنه: `Models/ Actions/ DTOs/ Events/ Listeners/ Services/ Policies/ Exceptions/`

**قاعده:** کنترلر فقط ورودی را اعتبارسنجی می‌کند و یک `Action` را صدا می‌زند. هیچ منطق کسب‌وکاری
در کنترلر، در مدل یا در Livewire نوشته نمی‌شود.

جزئیات: [۲۰-تحویل / ۰۱ — ساختار ریپو](../20-delivery/01-repo-structure.md)

---

## ۵. مسیر استقرار

| فاز | زیرساخت | ماهانه ‹ب› |
|---|---|---|
| ۱ — پایلوت | یک VPS ایرانی (۴ هستهٔ CPU، ۸ گیگ رم)، همه‌چیز روی همان | ~۲ م تومان |
| ۲ — ۱۰ سالن | همان + Redis + پشتیبان‌گیری خودکار روز‌به‌روز | ~۳ م |
| ۳ — ۱۰۰ سالن | وب و دیتابیس جدا · ۲ نود وب پشت لودبالانسر · Reverb | ~۱۲ م |
| ۴ — مارکت‌پلیس | + نود جستجو · رپلیکای خواندنی · CDN داخلی | ~۳۰ م |

تا فاز ۳ نیازی به کوبرنتیز یا میکروسرویس نیست و ساختنشان اشتباه است.

---

## ۶. نکته‌های عملیاتی که از `tools/host-check.php` می‌آید

ابزار موجود در ریپو، همین حالا چیزهای درستی را می‌سنجد و برای نصب روی هاست اشتراکی معتبر است:

| چیز | چرا مهم است |
|---|---|
| افزونهٔ `soap` | ملی‌پیامک با SOAP کار می‌کند — بدون آن **هیچ پیامکی نمی‌رود** و فقط وقتی می‌فهمی که اولین پیامک نرود |
| خروجی HTTPS | بدون آن نه درگاه کار می‌کند نه پیامک |
| `zip` | خروجی اکسل گزارش‌ها |
| `memory_limit` ≥ ۲۵۶M | گزارش و آپلود عکس |
| توابع غیرفعال | بعضی هاست‌های ایرانی `fsockopen` و `proc_open` را می‌بندند |

**تصمیم:** این ابزار نگه داشته می‌شود و برای رشن به‌روزرسانی می‌شود — به‌عنوان گام صفرِ هر نصب
روی هاست مشتری. برای استقرار VPS خودمان، Docker Compose داریم و این لازم نیست.

---

## ۷. پنجرهٔ ممنوعهٔ استقرار

> **پنجشنبه ۱۶:۰۰ تا ۲۲:۰۰ و جمعه ۱۰:۰۰ تا ۱۴:۰۰ — هیچ استقراری.**

این شلوغ‌ترین ساعات کل هفتهٔ آرایشگاه مردانه است. یک قطعی ۵ دقیقه‌ای در این بازه، بیشتر از یک ماه
قطعی در سه‌شنبه صبح به اعتماد آسیب می‌زند. در CI به‌شکل یک بررسی خودکار اعمال می‌شود.
